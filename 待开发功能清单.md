# 待开发功能清单

> 来源：2026-09-24 全项目代码审查（安全 / TypeScript / 前端三路专项）。
> 排序原则：优先变现已有数据资产（GPS、EXIF、shotAt、收藏），其次补齐画廊类产品的核心浏览体验，最后是锦上添花。
> 状态标记：⬜ 待开发 · 🚧 开发中 · ✅ 已完成

---

## P0 — 低垂果实（数据已入库，直接变现）

### ✅ 1. 地图视图
- **为什么**：GPS 坐标 + Photon 地名已全部入库（`Photo.exif.gps`），是目前最大的未变现数据资产。"我去过哪、在哪拍的"是个人画廊天然的第二浏览轴。
- **怎么做**：MapLibre/Leaflet + 坐标聚合点（cluster），点击聚合展开该地照片缩略图；入口加在视图切换（ViewToggle）或 Sidebar。
- **涉及**：`src/lib/queries.ts`（按 GPS 非空筛选）、新页面 `/map`、`ViewToggle`。
- **注意**：地图瓦片源需国内可达（同 Photon 替换 Nominatim 的教训）；底图选 OSM 兼容源或高德。
- **完成记录（2026-09-25）**：`/map` 路由 + Leaflet 1.9.4（JS 在 useEffect 内动态 import——Next 官方 lazy-loading 模式不进 SSR bundle，CSS 顶层静态引入）。纯函数层两块：`src/lib/geo-transform.ts`（`wgs84ToGcj02`——高德瓦片为 GCJ-02 坐标系，EXIF 的 WGS-84 直接落点偏几百米；eviltransform 发表算法，测试期望值取自独立 python 实现逐点对拍（深圳/北京/乌鲁木齐/拉萨四城 + 境外恒等 + 量级 <0.01° 约束），8 单测；反向 GCJ→WGS 无闭式解刻意不做）与 `src/lib/map-cluster.ts`（`clusterPoints`——zoom 相关经纬网格聚合 cellDeg=360·80px/(256·2^zoom)，簇心=成员均值，Map 插入序=输入序确定性输出，7 单测；聚合逻辑留在 vitest 可测的 src/lib 层而非 leaflet.markercluster，Leaflet 侧只做渲染）。`listMapPoints`（queries.ts）：隐私闸门与 listPhotos 同源 `canSeeGps`（exposeGps=false 且非管理员 → 整体空数组），raw SQL 抽 `exif.gps.lat/lon/location` + published/missing 过滤，`Number.isFinite` 防御非法坐标。`/map` 服务端页：瓦片配置 env 服务端读取、props 下发（`MAP_TILE_URL`/`MAP_TILE_SUBDOMAINS`/`MAP_TILE_GCJ02`/`MAP_TILE_ATTRIBUTION`，默认高德路网免 key；天地图/OSM 配 GCJ02=false 走 WGS-84 直接落图；不用 NEXT_PUBLIC_* 避免配置烧进客户端 bundle）；空态措辞不区分"无 GPS"与"隐私开关关闭"，不向访客泄露管理端状态。`MapView` 客户端组件：zoomend 重聚合；divIcon 数字气泡（深色玻璃 + #f5b43c 描边对齐站点审美，html 仅插值数字无注入面）；弹窗 DOM API 构建、文本一律 textContent（title/地名是用户数据，永不 innerHTML），缩略图 4×3 网格链接 `/photo/[sha1]`、超出 +N 收尾；fitBounds 15 级封顶留街区上下文；卸载 disposed 守卫 + `map.remove()`。入口：Sidebar 核心入口卡片「地图」（IconMap 对齐既有 ICON/STROKE 规格；/map 是独立路由非过滤参数，ViewToggle 语义不符——它 replace 到 `/?view=`，故未采用清单原案的 ViewToggle 入口）。163 测试全过、tsc/eslint 干净。
- **评审收编（2026-09-25，0830394 评审 APPROVE with comments）**：M-1 `listMapPoints` WHERE 补 `gps.lon IS NOT NULL` + filter 显式 null 排除——SQL NULL 的 `Number(null)===0` 能穿过 `Number.isFinite`，半截 gps（有 lat 无 lon）会成 (lat,0) 幻影点且 fitBounds 把视野拉到西非，防御注释自称覆盖处恰有洞；M-2 MapView async IIFE 补 try/catch + 显式错误态——Leaflet chunk 加载失败（部署后 hash 失效 404）原为静默空白地图 + unhandled rejection，现渲染「重新加载地图」按钮（amber 规格对齐 PhotoGrid 失败态），重试经 `key={attempt}` 整体换新容器 div，绕开 Leaflet 半初始化 `_leaflet_id` 导致的 "already initialized"；L-1 attribution 消费点注记信任边界（Leaflet 归因控件走 innerHTML，`MAP_TILE_ATTRIBUTION` 为管理端 env 受信配置、入口须保持管理端专属）；L-2 `MAP_TILE_SUBDOMAINS` split 后逐段 trim（手写 "1, 2, 3" 拼出非法主机名瓦片静默全挂）；L-4 divIcon 标记补 `aria-label`——Leaflet `alt` 选项仅对 `<img>` 图标生效（Marker.js `tagName === 'IMG'` 守卫实证），键盘可聚焦的 divIcon 元素手动 `getElement()?.setAttribute`。L-3/L-5/L-6 记技术债清单。评审确认无 CRITICAL/HIGH：StrictMode 双挂载生命周期、XSS 面（html 仅数字/textContent/encodeURIComponent）、SQL 注入（零插值 $queryRaw）、eviltransform 系数逐行核对、GPS 隐私闸门均验证可靠。

### ✅ 2. "那年今日" / 随机漫游
- **为什么**：shotAt 数据完整，成本极低，是个人画廊的复访驱动力。
- **怎么做**：首页顶部 memories 卡片（N 年前的今天拍的照片，横滑）；或"随机看 10 张"按钮（应用层 reservoir sampling，避免 `ORDER BY RAND()` 全表扫）。
- **涉及**：`src/app/(site)/page.tsx`、`src/lib/queries.ts`、`src/lib/time.ts`（DISPLAY_TZ 换算）。
- **完成记录（2026-09-24）**：默认首页渲染 MemoriesStrip"那年今天"横滑条（`listMemories` 只对有照片的往年各发一个走 shotAt 索引的日界查询并行执行，每年最多 12 张——全局 take 会让最近的大日子占满配额挤掉更早年份，评审后改为按年分查；`listYears` 加 React cache 防同请求重复全表聚合）；随机漫游 = `listRandomPhotos` 两段查询（轻量 id 候选池 ≤2 万 → `reservoirSample` 水塘抽样 10 张 → 按 id 回取卡片），`/?random=<nonce>` 每次点击新 nonce 绕开路由缓存实现"换一批"，入口在侧栏核心卡片 + 移动端 chips 栏 + 随机模式标题栏（激活态 aria-current）。`monthDayBoundsInYear` 用 displayMonthDay round-trip 校验拒绝 Date 归一化的非法日期（平年 2-29 → null 而非漂移到 3-01）。新增 `src/lib/sample.ts`、`src/components/MemoriesStrip.tsx`、`src/components/RandomWalkLink.tsx`，14 个新单测（time 7 + sample 7），全量 102 测试通过；评审 APPROVE，M-1~M-4/L-1~L-8 已全部落实或注释记录。

### ✅ 3. 按器材浏览（相机 / 镜头筛选）
- **为什么**：EXIF 已含相机、镜头、焦段、光圈；摄影爱好者画廊的标配维度。
- **怎么做**：Sidebar 增加"器材"分组（camera+lens 去重列表）；筛选逻辑复用现有 tag/category 的 URL 参数模式。
- **涉及**：`src/components/Sidebar.tsx`、`src/lib/queries.ts`（exif.camera/lens 过滤，Json 字段查询注意 MariaDB JSON 函数与索引）。
- **完成记录（2026-09-24）**：URL 参数词汇表扩展 `make`+`model`（相机=双维度定位——组合展示名「Canon EOS R5」不可靠反拆，不以 label 做参数）+ `lens`（→ `$.lensModel`）。纯函数层 `src/lib/gear.ts`：`parseGearParam`（trim + 空→undefined + 100 字符截断，与 q 同款防御）、`cleanGearValue`（`JSON_UNQUOTE(JSON_EXTRACT())` 的三种缺失形态——SQL NULL / 字符串 "null"（JSON null 字面量）/ 空串——统一清洗）、`buildGearCameras`/`buildGearLenses`（bigint→number；展示名复用既有 `formatCamera` 不重造；count 降序 + 名称升序）、`gearWhere`（Json path 等值条件收敛进**单个 AND 数组**——单顶层键与 buildListWhere 其余键零碰撞；空筛选返回 `{}` spread 零副作用），9 个新单测锁定。queries.ts：`ListOptions` 加 make/model/lens；`buildListWhere` 挂 `gearWhere`（**纵深防御**：入口已 parseGearParam 清洗、此处再清洗一次；`getAdjacentPhotos` 自动同享）；`listGear = cache(…)` raw SQL 一次并行聚合相机/镜头两维（GROUP BY SELECT 别名，MariaDB 支持，与 listMonths 同款），请求级去重。filter-url.ts：FilterKey/FILTER_KEYS/`PhotoContext`/`contextToParams` 扩展（插在 fav 与 view/month 之间，既有键相对顺序不变、既有单测零波及）→ **PhotoGrid 零改动**（loadMore 参数与 photoHref 经 PhotoContext 自动透传）。Sidebar「器材」分组：IconCamera/IconAperture 线性图标对齐既有 ICON/STROKE 规格；相机项 href 走 clear 语义（点相机自动清掉 lens 等其余维度）；无器材数据整组隐藏。首页：gearLabel h1（相机 · 镜头「 · 」连接）、「全部」chip 与激活态含三新维度、PhotoGrid key/query 补维；详情页 navQuery/getAdjacentPhotos 透传；/api/photos 透传。BigInt 字面量以 `BigInt(n)` 写法适配 ES2017 target。142 测试全过、tsc/eslint 干净。
- **评审收编（2026-09-25，9d55ae2 评审 APPROVE with comments）**：M-1 filter-url.test.ts 补器材词汇表（make/model/lens）6 测——Sidebar 相机点击 clear 语义连带清 lens、patch 显式 `model: undefined` 清除 model、移动端「全部」chip keep 模式一次清空三维、FILTER_KEYS 规范顺序哨兵（重排即挂测试）、contextToParams 维度位于 fav 后 month 前、photoHref 编码往返：词汇表扩展本应与 gear.ts 同提交覆盖，属遗漏；L-1 `cleanGearValue` 补依赖注记——trim 只作用于展示标签、`gearWhere` equals 匹配原始存储值，一致性前提是写路径 `extractExif→cleanString` 入库前已 trim（历史未 trim 行边缘记技术债）。评审确认无 CRITICAL/HIGH；d9c901f（H-1/L-1 修复）同轮评审 APPROVE，L-5/L-6 良性并发窗口与既有边缘记技术债。

### ✅ 4. 幻灯片模式
- **为什么**：全屏自动播放是画廊产品的经典体验，纯前端即可实现。
- **怎么做**：详情页或灯箱内加"播放"入口；自动切换 + 淡入过渡（只用 transform/opacity，合成器友好）；尊重 `prefers-reduced-motion`（globals.css 已有全局降级先例）。
- **涉及**：`src/components/ZoomableImage.tsx` 或新组件 `Slideshow.tsx`。
- **完成记录（2026-09-25）**：入口=详情页「▶ 幻灯片」胶囊按钮（新组件 `src/components/slideshow/SlideshowButton.tsx`；ZoomableImage 未动——它只管单张缩放，塞播放列表会职责膨胀）。**播放列表零 queries.ts 改动**：点开时客户端取 `/api/photos?contextToParams(navQuery)`（与 PhotoGrid.loadMore 同一参数词汇表、单一出处），`buildPlaylist` 从当前照片截到页尾——列表序与详情页 ←/→ 相邻导航一致；播到末尾 `nextIndex` 回卷循环；当前照片不在上下文第一页（深滚动/直链进入）时回退置顶插入。纯函数层 `src/lib/slideshow.ts`（`toSlideshowPhoto` 挑 3 字段新对象 / `buildPlaylist` / `nextIndex`/`prevIndex` 越界归一+回卷 / `SLIDE_INTERVAL_MS=4000` 测试锁定），17 单测。过渡 `.slideshow-fade` 只动 opacity 600ms（globals.css 新增 keyframes，合成器友好）；prefers-reduced-motion **双保险**：全局块自动压 animation-duration 至 0.01ms + `start`（点击处理器）判定 reduced-motion 即暂停态打开（WCAG 2.2.2 自动轮播即动效，空格/播放键随时可恢复；判定放处理器而非挂载 effect——effect 内同步 setState 级联渲染且首帧空转一次计时器，eslint react-hooks/set-state-in-effect 实证后重构）。键盘：Esc 退出、←/→ 换片（重置自动计时）、空格播放/暂停（preventDefault 压掉焦点按钮原生激活防"切播放+触发按钮"双动作），守卫对齐 PhotoNav（defaultPrevented/修饰键/输入控件/富文本不劫持）。放映期间 `body[data-lightbox="1"]` + overflow:hidden（既有约定，PhotoNav/BackOnEsc 自动让位）+ 关闭时恢复原 overflow。取数 AbortController：关闭/重试/重开/卸载都先 abort 上一发，catch 判 `signal.aborted` 不算错——杜绝旧响应覆盖新列表竞态；失败不静默：错误态 + amber 重试按钮（PhotoGrid/MapView 同款姿态）。放映用 `displayUrl` WebP 变体而非 zoomSrc（originalView 开启时那是原图直链，HEIC 浏览器无法渲染——详情页回退逻辑同款理由）；`new Image()` 预取下一张防淡入空窗；顶栏计数 aria-live 播报换片、`key={sha1}` 换片重挂载触发淡入。188 测试全过、tsc/eslint 干净（仅既有可容忍 no-img-element warning）。
- **技术债**：播放列表仅上下文第一页（settings.pageSize 张，默认 24）——播完循环回卷、不拉续页（/api/photos 路由不接受 pageSize 参数，续页需分页循环或专用服务端查询）；当前照片不在第一页时只回退置顶、不跨页寻找其真实位置；无焦点陷阱（与既有 ZoomableImage 灯箱姿态一致，Tab 可逸出覆盖层）；单张图片加载失败仅 alt 文本兜底、自动播放径直下一张（无 per-image 错误态/重试）；未走 Fullscreen API（z-50 覆盖层而非真全屏——刻意：避开权限提示与跨浏览器差异）；入口仅详情页（列表页无"从这里开始放映"，移动端=详情页按钮天然可用）；`ZoomableImage.tsx` 的 × 关闭按钮无 h-11/w-11 触控尺寸（与本次 L-4 修复前的 SlideshowButton 同源弱点，改动在评审范围外文件、记债）。
- **评审收编（2026-09-25）**：cc01bfc 评审 APPROVE with comments，独立核实代理逐条以 file:line 证实后收编 6 项——**M-1** 根 `onClick={close}` 永不可达（顶栏/舞台/底栏三容器全 stopPropagation 且纵向铺满无裸露像素，与 ZoomableImage「点图不关、点黑幕关」灯箱惯例相悖）→ stopPropagation 下沉到交互子元素（×/重试/上一张/下一张/播放暂停）与 `<img>`，容器不再拦截、黑幕点击冒泡退出；**M-2a** catch 中 `aborted` 早退不跳过 `finally { setLoading(false) }`——fetch 在途再触发 start()（Enter 重激活路径）时旧 IIFE 会把新一轮 loading=true 踩回 false（旧图闪现/黑屏空窗）→ finally 加 `!ac.signal.aborted` 守卫；**M-2b** 焦点留在被覆盖的「▶ 幻灯片」入口按钮，Enter 原生激活静默重播（空格已 preventDefault、Enter 不对称）→ 根对话框 `ref + tabIndex={-1}`，打开 effect 中 `focus()` 迁入；**L-1** 键盘守卫缺 `e.repeat`/`e.shiftKey`（提交信息声称「对齐 PhotoNav」但 PhotoNav.tsx:32-33 两者皆有）→ 补齐；**L-2** 重启加载期间顶栏显示旧计数/标题（start 全程不碰 playlist/index，顶栏无 loading 门控而舞台有）→ 计数与标题加 `!loading &&` 门控；**L-3** 响应仅顶层 `Array.isArray`、元素级零校验（`as PlaylistResponse` 纯编译期断言 + toSlideshowPhoto 只挑字段）→ `items?: unknown` + `isPlaylistItem` 类型守卫逐元素校验 sha1/title/displayUrl 皆 string，坏元素静默丢弃；**L-4** × 按钮无触控尺寸 → 补 `flex h-11 w-11 items-center justify-center`（44px 触控标准）。核实报告两处自身瑕疵不影响结论：PhotoNav/ZoomableImage 路径实为 `src/components/`（报告误写 `photo/` 子目录）；「对齐 PhotoNav」声称出自提交信息而非组件注释。

---

## P1 — 核心浏览体验补齐

### ✅ 5. 详情页上一张/下一张 + ←/→ 键盘翻页
- **为什么**：画廊类产品核心浏览闭环，目前缺失；键盘导航同时是 a11y 要求。
- **怎么做**：服务端按当前筛选上下文（URL 参数透传）取相邻 sha1；`prefetch` 下一张。
- **涉及**：`src/app/(site)/photo/[sha1]/page.tsx`、`src/lib/queries.ts`、可参考 `BackOnEsc` 的键盘监听模式。
- **完成记录（2026-09-24）**：与功能 6 合批交付。`getAdjacentPhotos(sha1, options)` 与 `listPhotos` 共用抽出的 `buildListWhere`（同一套筛选语义），相邻排序=列表序（shotAt desc, createdAt desc），MariaDB NULL 排序语义（ASC 前/DESC 后）与列表隐式行为对齐、shotAt 为 null 走 createdAt 兜底；相邻条件与 where（q 会产生顶层 OR）用 AND 组合防键覆盖。上下文透传：`photoHref(sha1, ctx)`（filter-url.ts，category/tag/year/q/fav/month 规范序，5 单测锁定），PhotoGrid 六处详情链接统一走它（含 PhotoCard 增加 href prop），首页/分类/标签三个列表面自动覆盖；MemoriesStrip/管理端等无上下文入口=全库默认列表序。新组件 `PhotoNav.tsx`：悬浮 ‹/› 圆钮（aria-label 含目标标题）+ ←/→ 键盘导航（防御对齐 BackOnEsc：灯箱 body[data-lightbox] 打开、输入控件/富文本聚焦、修饰键按下时不劫持）+ 下一张 prefetch。`getPhotoDetail` 加 React cache（metadata 与页面主体同一请求去重）。

### ✅ 6. 详情页 SEO / 社交分享卡片
- **为什么**：目前照片页无 `generateMetadata`，分享到社交平台没有任何卡片；照片本身就是最好的分享素材。
- **怎么做**：导出 `generateMetadata`（标题=照片标题、描述=EXIF 摘要、openGraph.images=display URL）；sitemap 顺带把 category/tag 页也加进去。
- **涉及**：`src/app/(site)/photo/[sha1]/page.tsx`、`src/app/sitemap.ts`。
- **完成记录（2026-09-24）**：详情页导出 `generateMetadata`——可见性守卫收编为 `resolvePhoto = cache(regex 校验 + published/missing + isAdmin)`，不可见照片返回空 metadata（分享卡片不泄露未发布标题/图像）；title 由根布局 template 拼「标题 · soloGallery」；description=手工描述（截 200）?? EXIF 摘要（器材 · 焦段 · 光圈 · 快门 · ISO · 日期，复用 exif-format 纯函数）；openGraph type=article + canonical + images=displayUrl（MinIO 公共读绝对地址，含 width/height/alt）；twitter summary_large_image。`siteUrl()` 收编进 config.ts（原 sitemap 内联 SITE_URL 逻辑，DRY）。sitemap 增加 category（priority 0.6）/tag（0.5，encodeURIComponent）条目，三查询并行、各自 .catch 回退空数组。验证：107 测试全过（+5 photoHref）、tsc/eslint 干净。
- **评审收编（2026-09-24，bfa41fc 评审 APPROVE、无 CRITICAL/HIGH）**：M-1 `firstParam` 归一 searchParams 重复参数（详情/首页两页，`?q=a&q=b` 运行时是数组、直调 .trim() 即 500）；M-2 `parseYear`（1971..9998）首页/详情页/`/api/photos` 三处同源，拒 5 位年份的 Invalid Date 进 Prisma，并消除页面无上界而 API 有 `<9999` 的漂移；M-3+L-3 PhotoNav 键盘守卫补 `e.repeat`（长按 ~30Hz 连发导航、污染历史栈）与 `e.shiftKey`；M-4 sitemap 分类 slug `encodeURIComponent`（slugify 保留 `\p{L}`，Next 序列化器原样插 `<loc>` 不转义）；L-1 OG width/height 按 `DISPLAY_WIDTH=1920` 等比折算（displayUrl 是缩小变体，声明原图尺寸致纵横比失真）；L-4 description 按码点截断（`Array.from` 防切断 emoji 代理对）；L-6 sitemap 过滤 count=0 空列表页；L-7 resolvePhoto 前缀查询加 `orderBy:{id:asc}`（短前缀碰撞时选择确定）；L-8 抽 `contextToParams` 供 photoHref 与 PhotoGrid.loadMore 共用（参数词汇表唯一出处）；L-2 prefetch 注释如实化（dynamic 路由 prefetch 只热到 loading 骨架）+ effect 依赖改稳定 `next?.href`。新增 10 单测（firstParam 3 + parseYear 4 + contextToParams 3），117 全过、tsc/eslint 干净。
- **评审收编（第二轮，2026-09-24，1aab282+da1e80d 评审 REQUEST CHANGES）**：H-1 `getPhotoDetail` 前缀查询补 `orderBy:{id:"asc"}`——短前缀碰撞多行时与 `resolvePhoto` 的可见性判定选中同一行（同 min-id），消除"判定行已发布、渲染行未发布"的理论泄露路径（published/missing 并入 detail 单查询的彻底方案仍留技术债）；M-1 首页三处 chip 激活判断（分类/年份/标签）改读 `firstParam` 归一后的局部量，全页 `sp.*` 裸读清零；M-2 管理端图片管理页（firstParam/parseYear 的第四个旧校验消费方）收编同款归一——重复参数不再 500、`?year=99999` 不再让 yearBounds 产出 Invalid Date 进 Prisma，`listPhotosAdmin` 补信任边界注释（year 必须由调用方经 parseYear）；L-6 详情页与 PhotoCard 的 category slug 链接统一 `encodeURIComponent`（对齐 tag 链接与 sitemap 先例）；L-9 tag 页 `decodeURIComponent` 包 try/catch → `notFound()`（畸形 `%zz` 不再落 error.tsx 呈 500 观感，对齐 category 页先例）。130 测试全过、tsc/eslint 干净。

### ✅ 7. 路由级 UI 边界（loading / error / not-found）
- **为什么**：全站目前没有任何路由级 UI 边界：force-dynamic 页面切换无 loading 态、渲染异常直接白屏、404 是 Next 默认页（与暗色玻璃风格不匹配）。
- **怎么做**：`(site)/loading.tsx`（骨架网格）、`(site)/error.tsx`、`(site)/not-found.tsx`，风格对齐现有设计 token。
- **涉及**：`src/app/(site)/`。
- **完成记录**：2026-09-24 —— `(site)/loading.tsx` 骨架屏对齐 fixed 视图网格与侧栏玻璃面板；`(site)/error.tsx` 玻璃卡片错误页（Next 16 `{ error, retry }` 约定，UI 展示 digest 供对照服务器日志）；`(site)/not-found.tsx` 段内 404（notFound() 触发，含站点 chrome）；`app/not-found.tsx` 根级 404（未匹配 URL 渲染在根布局，自包含整页）。

### ✅ 8. 移动端标签筛选入口
- **为什么**：tags 只存在于 `hidden lg:block` 的 Sidebar，手机上标签维度无入口（只能从照片卡片反点）。
- **怎么做**：移动端 chips 栏加"标签"抽屉/横滑 chips；或筛选面板底部弹出。
- **涉及**：`src/app/(site)/page.tsx`（chips 区）、`src/components/Sidebar.tsx`。
- **完成记录（2026-09-24）**：采用横滑 chips 方案——chips 栏下新增独立「标签」横滑行（`lg:hidden`，行首"标签"小字标注维度），复用 `FilterLink` + `qs()` 的叠加筛选语义（与分类/年份 chips 同一套 `buildFilterUrl` unset:"keep" 行为，点击即退出日历/单月视图）；激活态 `sp.tag === t.name` 与 Sidebar 标签分组同语义，chip 带计数。纯服务端渲染、零客户端 JS、无新纯函数（tag 参数已被 filter-url 既有单测覆盖）；Sidebar 无需改动。107 测试全过、tsc/eslint 干净。

---

## P2 — 管理端与性能优化

### ✅ 9. 管理端乐观更新
- **为什么**：目前任何单操作（收藏、发布开关）都置全局 busy 禁用整个网格，等 `router.refresh()` 整页往返才恢复。
- **怎么做**：本地先行更新 + 失败回滚 + 可见错误反馈；顺带把 4 个 admin Client 的共性抽成 `<ErrorBanner>` + `useAdminAction()`。
- **涉及**：`src/components/admin/AdminPhotosClient.tsx` 等。
- **完成记录（2026-09-25）**：三层落地。**纯函数层** `src/lib/optimistic.ts`（patchById/removeById/insertAt——行级原语，全部不可变、未命中行保持原引用；为什么不用快照整体回滚：两行并发乐观操作时先失败者的快照回滚会 clobber 后发者的中间态，行级函数式回滚只碰目标行、天然与并发可交换；14 单测含「反向补丁还原」「removeById+insertAt 原位插回恢复原序与行引用」闭环用例）。**公共状态机** `useAdminAction()` hook（busy 门 + error 收集 + 成功回流三件套——原先 4 个 Client 内联同一段 setBusy/setError/try-catch-finally/refresh 样板 8+ 处，且 busy 覆盖不一致：Tags/Categories 的删除、重命名根本不置 busy，双击可重复发 DELETE，收敛后语义统一并补齐；`run(url, init?, opts?)→Promise<boolean>`，opts：onSuccess（refresh 前清输入/清选择）、quiet（乐观调用方自管 per-item pending 不置全局 busy）、refresh（默认 true）；false 返回=调用方回滚，error 已由 hook 写入）+ `jsonInit()` 收敛 7 处 JSON RequestInit 样板 + `<ErrorBanner>` 组件（同段红玻璃标记 4 处内联收敛，顺带补 role="alert"——内联版读屏器不播报）。**接线**：AdminPhotosClient ★收藏/发布开关乐观翻转（本地镜像 photos + 渲染期 prop 变化重置——React 官方「adjust state when props change」模式，放 effect 会挂 react-hooks/set-state-in-effect；行级 pendingIds 只禁目标照片控件，全局 busy 仅批量操作时冻结工具栏；失败反向补丁回滚本行）；CommentsClient 通过/标垃圾/回复乐观 PATCH + 删除乐观行消失（失败按记录 index 原位插回；回复草稿在服务端确认后才清空，失败可直接重试）；EditModal save 走 hook（busy 别名 saving，加载详情 GET 保留独立 loading/loadFailed 态与数据丢失防护）；Tags/Categories 不做乐观（创建需服务端生成 id/计数）但补上缺失的 busy 门。202 测试全过、tsc/eslint 干净（仅既有 no-img-element warning）。
  - 技术债：SyncClient/SettingsClient 未收编 hook（清单只点名 4 个 Client；UploadClient 已在功能 12 重做时裁决为**刻意不收编**——XHR upload.onprogress 字节级进度与逐项状态机装不进 fetch 形状的 hook，详见功能 12 完成记录）；乐观更新无「行级 in-flight 视觉指示」（pending 期间仅禁用控件，无 spinner/半透明）；批量操作（含批量删除）仍走全局 busy + refresh 非乐观；照片乐观翻转成功后仍触发 router.refresh() 整页回流（保守一致性兜底，高频连点时会放大服务端负载）；CommentsClient 镜像与分页无关（管理端评论页现全量渲染，若未来分页需连同 initial 语义重审）；**跨操作 refresh 闪回窗口（评审 M-2）**：行 X 成功的 refresh 若在服务端渲染早于行 Y 的提交，回流快照不含 Y 的乐观值——镜像重置把 Y 闪回旧值直到 Y 自己的 refresh 到达（≈一次 RSC 往返，自愈但高频连点下肉眼可见抖动；pendingIds 独立于镜像不受影响，闪回期间控件仍禁用）——修法三档成本递增：重置时对 pending 行重放乐观 patch / refresh trailing-debounce 合流（顺带消化上一条负载放大）/ 迁移 React 19 useOptimistic；单槽 error 并发覆盖（评审 L-5）：两个并发 quiet 操作都失败时横幅只留最后一条，先失败行只余回滚无解释（单横幅设计固有取舍，如要改进收集 error 数组）；ErrorBanner 条件挂载（评审 L-4）：alert 节点连内容一次性插入 DOM，部分读屏器对插入型 live region 播报不稳，更稳形态是容器常驻 + 内容切换；`tags/[id]`/`categories/[id]` DELETE 无 isPrismaNotFound 处理（评审观察项，超出功能 9 范围）：删除成功后 refresh 到达前的窗口内对同行再确认删除会 P2025→未捕获→500，photos/[id] DELETE 已幂等、comments/[id] 已返 404 友好消息，二者未对齐，后续统一。
- **评审收编（2026-09-25）**：feda644 评审 APPROVE with comments（无 CRITICAL/HIGH；验证 202 测/tsc 0 错/eslint 仅既有 warning 与提交声称逐项吻合，量化声称「7 处 jsonInit」「4 处红玻璃」「8+ 处样板」核实准确，渲染期 setState 模式与 React 官方 adjust-state-when-props-change 文档逐字吻合、StrictMode/并发中断安全）。收编项——**M-1** 删除失败回滚可产生重复行：DELETE 失败=服务端行始终在，并发操作成功的 refresh 已把它随 props 带回（镜像重置后复活），再 `insertAt` 即重复行/key 冲突 → 下沉幂等纯函数 `insertIfAbsent`（已在则返回原引用触发 React bail-out）+ 2 条交错单测（行不存在原位插回 / 已被带回幂等不重复）；**L-1** 失败回滚可覆盖重置后的新真值且失败路径无 refresh 兜底 → hook 暴露 `refresh`，AdminPhotos/Comments 的 `!ok` 分支回滚后 `refresh()` 收敛服务端真值（顺带部分缓解 M-1/M-2）；**L-2** 注释失实修正：「删除双击可重复发 DELETE」不可达（ConfirmDialog 自带 busy 门+模态遮罩+await 完成才关闭），真实缺口仅 Categories 行内重命名——useAdminAction 头注释/TagsClient/CategoriesClient 三处措辞改为「重命名可重复发 PATCH；删除路径对话框已有门、hook 是第二层纵深防御」；**L-3** EditModal 注释描述不可达路径（「run 开始 setError(null) 恰好清掉陈旧加载错误」——loadFailed 时保存禁用 run 永不启动）→ 改为「加载错误保留至关窗」；**L-4** 模态内错误 `<p>` 补 role="alert"；**L-6** patch 类型收窄：togglePhoto 用 `Partial<Pick<AdminPhotoDTO, "favorite"|"published">>`、patchComment 用 `Partial<Pick<CommentAdminDTO, "status"|"adminReply">>`（宽于服务端白名单的字段会被乐观应用但被静默忽略）；`jsonInit` method 收窄为 `"POST"|"PATCH"|"DELETE"` 字面量联合。**M-2/L-5/L-4（常驻容器）/tags-categories DELETE 观察项记技术债**（见上）。评审确认安全项：pendingIds 函数式更新无丢失、run 永不 throw 故 withoutPending 必然执行、quiet 混合并发不污染 busy、成功路径不清 error（并发一成一败时失败消息不被顶掉）、EditModal 数据丢失防护逐项等价保留、全部请求契约与 route 源码逐字段匹配、零 console。

### ⬜ 10. 图片多尺寸 srcset
- **为什么**：所有视图共用单一 thumb 变体，square/list 小图位与大屏 masonry 下载同样字节数。
- **怎么做**：image-pipeline 增加 thumb 的第二尺寸档（如 400w/800w），`srcset` + `sizes` 输出；配合详情页主图 width/height 修复（CLS）。
- **涉及**：`src/lib/image-pipeline.ts`、`src/lib/bucket-layout.ts`、`src/components/PhotoGrid.tsx`。

### ✅ 11. 无限滚动兜底交互
- **为什么**：加载失败目前静默卡死（IO 不重试）、无手动"加载更多"、无回到顶部。
- **怎么做**：失败置 error state → 渲染"加载失败，点击重试"；底部加回到顶部按钮。
- **涉及**：`src/components/PhotoGrid.tsx`。
- **完成记录（2026-09-24）**：`error` 态替代静默 catch——loadMore 失败置 error，底部渲染「加载失败，点击重试」琥珀描边按钮（对齐详情页未发布横幅警示配色；min-h-11 触控目标 + `#f5b43c` focus ring）；重试入口在尝试开始即清 error，用户滚离使哨兵重进视口也可经 IO 自动重发（修掉"哨兵位置不变 IO 永不重触发"的卡死根因）。回到顶部：网格顶部 1px 哨兵 + IntersectionObserver（rootMargin 200px 缓冲）驱动 showTop，底部按钮沿用 PhotoNav 玻璃 token（border-edge bg-background/70 backdrop-blur text-muted hover:text-foreground）；scrollToTop 显式 `matchMedia("(prefers-reduced-motion: reduce)")` 判断（globals.css 降级只覆盖 CSS 动画，JS 平滑滚动需自判），reduce 时 behavior:"auto"。hasItems 作顶部哨兵 effect 依赖（空列表提前返回路径不渲染哨兵，空→非空时重挂 observer）。纯交互态（React state + IO/matchMedia/scrollTo 浏览器 API）无可提纯函数（vitest glob 仅覆盖 src/lib），同功能 8 先例不新增单测；130 测试全过、tsc/eslint 干净（仅既有 no-img-element warning）。
- **评审收编（2026-09-24，28e544d 评审 REQUEST CHANGES）**：H-1 失败态无限自动重试——`loading` 在 `loadMore` 的 useCallback deps 中，失败后 loading 翻转 → loadMore 新身份 → 底部哨兵 effect 重跑 → IntersectionObserver 重挂 → 规范强制的初始回调 → 哨兵仍在 600px 缓冲区内 → 自动重发 fetch，持续失败即成无退避死循环（error UI 还被下一轮尝试开始的 `setError(false)` 闪烁清除）。修复：`errorRef = useRef(false)` 作 error 的 ref 镜像（ref 读取不参与 callback 身份），catch 同步置位、IO 回调加 `!errorRef.current` 守卫、重试按钮先清 ref 再调 loadMore——重试收敛到显式按钮，observer 重挂只产生一次空转。语义变化：滚离再滚回不再自动重发（该路径正是死循环成因）。

### ✅ 12. 上传体验完善
- **为什么**：失败项不能单独重传、无完成汇总、无单文件超限提示。
- **怎么做**：每项加"重试"按钮；队列结束显示成功/跳过/失败汇总。
- **涉及**：`src/components/admin/UploadClient.tsx`。
- **完成记录（2026-09-25）**：纯函数层 `upload-queue.ts`——`MAX_FILE_BYTES=30MB` 单一出处：客户端入队预检 + 服务端 route.ts 兜底（本地常量迁移为共享导入，超限错误文案消费点输出逐字不变，绕过前端直连 API 依然被拒）；`isOversized` 上限含边界（> 才拒，与服务端比较语义一致）；`summarizeQueue` 按状态计数 + `settled`（队列非空且全部到达终态）供汇总条展示时机——入参收敛为结构子集（{status}/{size}），11 单测。UploadClient 重写：队列项改**单调递增 id 寻址**（原实现按 File 引用定位 + name+idx 作 key，同名文件 key 冲突；id 为 React key 与逐项重试的唯一依据，自增放 setQueue updater 之外防 StrictMode 双调用重复计数）；**超限文件入队即标 error**「超过 30MB 上限，未上传」（不进 pending、零带宽浪费；uploadOne 发送前二次 isOversized 拦截兜底重试路径）；提取 `uploadOne(item)` 单文件 XHR 封装（onprogress 字节级进度 → onload 解析 outcomes[0] → 终态），整批与单项重试共用；startUpload 显式快照 pending 目标（循环期间拖入的新文件留给下一轮，不依赖闭包 queue 恰好不变的隐含前提）；retryOne 与整批共用 running 门防并发双 XHR；**「清除已完成」保留 pending+error**（原实现只留 pending——失败项被静默清掉、连带销毁唯一重试入口）；汇总条 settled && !running 时渲染 role=status「成功 X · 跳过（已存在）Y · 失败 Z」（零计数段隐藏）；updateItem 复用功能 9 `patchById`（行级补丁原语 DRY）；**刻意不收编 useAdminAction**（裁决功能 9 技术债备忘：hook 面向 fetch+JSON 三件套，上传需要 XHR 进度事件与逐项状态机，强行适配会丢进度）。215 测试全过、tsc/eslint 零错误零警告。
  - 技术债：顺序上传无并发池（多图大批量总耗时=各文件之和，可做并发上限 2-3 的调度）；无失败项「全部重试」整体入口（大批量失败需逐个点）；uploadOne 无 AbortController/卸载防护（上传中离开页面 XHR 继续耗流量，setState 落已卸载组件为 no-op——管理端场景主动离开低频）；拖拽未递归遍历目录（webkitGetAsEntry 未做，拖入文件夹取不到内容）；exists 判定需服务端算 sha1 后才知（前端内容判重要读整文件，代价不值，不做）；汇总条无总耗时/速率信息。

### ⬜ 13. 评论分页 + 字数计数
- **为什么**：详情页一次性渲染全部 APPROVED 评论；表单 maxLength 2000 硬截断无提示。
- **涉及**：`src/components/CommentSection.tsx`、`src/lib/queries.ts`。

### ✅ 14. 评论 IM 通知（webhook）
- **为什么**：单人站长场景，新评论目前只能主动进后台查看；推送到 Server酱/Telegram bot 即时可知。
- **怎么做**：评论入库（APPROVED 或 PENDING）后异步 POST webhook URL（设置页可配）；失败不影响评论主流程（记日志）。
- **涉及**：`src/app/api/comments/route.ts`、`src/lib/settings.ts`、`SettingsClient.tsx`。
- **完成记录（2026-09-25）**：纯函数层 `notify.ts`（分层同 geo.ts：纯构造 + 带 logger 的副作用发送同文件）——`parseNotifyProvider` 白名单收窄（"" 关闭/未知值/大小写不符→null）；`truncateNotifyText` 按码点截断（emoji 代理对不劈开，不超长返回原引用），正文上限 300、标题中照片标题上限 20（Server酱 title 限 32）；`buildCommentNotifyMessage` 构造标题（待审核带「（待审核）」标记）+正文（昵称：内容摘要 + 详情页链接行）；`buildNotifyRequest` 纯构造请求体——Server酱 `{title, desp}`、Telegram `{chat_id, text, disable_web_page_preview}`，配置残缺（provider 未知/URL 空/telegram 缺 chatId）一律 null=视为关闭；`sendNotify` 永不抛出（AbortSignal.timeout 5s，非 2xx 与异常只 logger.warn）。设置三键进 `SETTING_DEFAULTS`（notifyProvider/notifyWebhookUrl/notifyChatId，默认全空=关闭→升级零行为变化），settings API `strings` 白名单收录 + 两条校验（provider 域复用 parseNotifyProvider 单一解析器；URL 仅 http/https——防 file:/javascript: 被 sendNotify 原样 fetch）。评论路由：photo select 扩 `{id,sha1,title}`（同表字段零额外查询），非 SPAM 入库后 `void sendNotify(...)` fire-and-forget（复用已取 settings 零额外 DB 读、不 await，推送延迟/失败不影响响应；SPAM 路径刻意不通知）；photoUrl 由 `new URL(request.url).origin + /photo/<sha1>` 拼装。SettingsClient：provider 下拉（关闭/Server酱/Telegram Bot）+ 条件渲染 webhook URL（placeholder 按 provider 给示例）与 chatId 输入 + 「推送失败仅记日志」提示；UI 刻意不 import notify.ts（避免把含 fetch 副作用模块拉进客户端包，服务端白名单校验兜底）。vitest 无 alias 教训重现：notify.ts 内部 import 必须相对路径（`./logger`）。16 单测直测纯函数层；231 测试全过、tsc/eslint 干净（仅既有 no-img-element warning）。
  - 技术债：无「发送测试通知」按钮（配置正确性只能靠真评论验证）；管理员后台回复评论不触发通知（只覆盖访客新评论）；Telegram 正文纯文本无 parse_mode（Markdown 转义负担不值）；推送失败无重试/队列（尽力而为语义，仅日志可查）；webhook URL（含 Server酱 SendKey/Telegram bot token）明文存 Setting 表且 GET /api/admin/settings 原样返回（仅管理员可见，与站点其余密钥同级——如介意可改环境变量注入）；sendNotify 在 serverless 环境可能被响应后冻结截断（本项目 nodejs runtime 自托管不受影响）。

---

## P3 — 探索性 / 锦上添花

### ⬜ 15. 相似照片检测（感知哈希）
- **为什么**：已有 sha1 精确去重，连拍/近似图需要 aHash/dHash 才能找出；sharp 可直接算，辅助清理库。
- **怎么做**：入库时计算 dHash 存库；后台"相似照片"页两两对比（汉明距离阈值）；提供批量保留最优删除其余。
- **涉及**：`src/lib/image-pipeline.ts`、Prisma schema（新增列）、新 admin 页面。

### ✅ 16. 年度归档页
- **为什么**：比日历更轻的回顾入口；`/archive/2025` 每月取收藏优先的 N 张拼成年度长页。
- **涉及**：新路由 `src/app/(site)/archive/[year]/page.tsx`、`src/lib/queries.ts`。
- **完成记录（2026-09-25）**：`/archive/[year]` 路由 + 纯函数层 `src/lib/archive.ts`（`buildArchiveYear`——monthKey 前缀过滤排除非本年行（纵深防御，取数侧 yearBounds 已保证）、零填充 ym 字符串序分组即月升序（年度长页像翻相册，与日历的月倒序刻意相反）、月内收藏优先经 ES2019 稳定排序保持输入序（shotAt 倒序）、perMonth 截断（缺省 `ARCHIVE_PER_MONTH=8`，4 列 × 2 行）、total 仍报该月全量供「共 X 张 · 精选 N」；DTO 边界剥离内部字段 monthKey；7 单测覆盖空输入/升序/收藏优先稳定/截断/跨年排除/不可变）。`listArchiveYear`（queries.ts）：listPhotosCalendar 同款轻量 select（sha1/thumbKey/title/favorite/shotAt），yearBounds 展示时区边界与 listYears 的 CONVERT_TZ 分组同一时间语义，shotAt 非空由 where 保证、空串兜底行会被纯函数层前缀过滤吃掉。页面：parseYear 与首页/详情页同源（1971..9998，非法/越界 404 不进 Prisma）；无照片的年份 404（与年份 chips 只列有照片年份一致）；上/下年导航按 listYears 降序相邻只链有照片的年份（天然不 404）+ 日历入口；月节 `aria-labelledby` +「查看本月全部 →」链 `/?month=`；缩略图沿用 CalendarView 卡片审美（aspect-[4/3] rounded-xl + group-hover scale-105 + ★ 收藏角标）；详情链接 `photoHref(sha1, {month})` 透传当月上下文供详情页 ←/→ 翻页；generateMetadata 用显式 `Props { params: Promise<{year: string}> }` 而非 `PageProps<>`——.next/types/routes.d.ts 的 AppRoutes 联合由 next dev/build 再生成，未注册新路由的泛型实参会挂 tsc（/map 页同款姿态）。入口：首页年份筛选 h1 分支「年度归档 →」上下文链接（Sidebar 年份分组保持过滤语义不变，未占用）。170 测试全过、tsc/eslint 干净（仅既有 no-img-element warning）。
- **评审收编（2026-09-25，d770f8a 评审 APPROVE with comments）**：M-1 archive.test.ts 补「收藏优先于截断」1 测——既有 7 测中收藏优先用例 perMonth≥行数（slice 空转）、截断用例零收藏（sort 空转），"先截断后排序"的回归变体可通过全部旧测试而在生产中静默丢掉落在截断线之外的收藏（评审者以变体实现实证），新用例钉住排序先于 slice 的实现顺序；L-1 首页「年度归档 →」改 `total > 0` 才渲染——手输 `/?year=1999`（0 张）原会给出 404 的 `/archive/1999` 死链；L-3 ★ 收藏角标 span 补 `role="img"`——无 role 泛型元素上的 aria-label 被辅助技术忽略，★ 字形会裸进链接可访问名。L-2/L-4 记技术债清单。评审确认无 CRITICAL/HIGH：yearBounds 与 CONVERT_TZ 年边界在相同 UTC 瞬时翻转（1986-1991 DST 不跨元旦）、并发下架竞态的 `idx===-1` 防御路径只降级不崩溃、排序稳定性是 ES2019 起的运行时规范保证（与 target ES2017 无关）、published/missing 门与日历/年份查询同源零泄露、listArchiveYear 零 raw SQL、photoHref month 上下文与 `/?month=` 同一 buildListWhere/monthBounds 语义。171 测试全过。

### ✅ 17. RSS / Atom 输出
- **为什么**：sitemap 已有，加 feed 让朋友订阅更新，成本低。
- **涉及**：新路由 `src/app/feed.xml/route.ts`。
- **完成记录（2026-09-24）**：`GET /feed.xml` 输出 RSS 2.0（选 RSS 2.0 + media 扩展而非 Atom：主流阅读器对 `media:content` 缩略图支持最好，且 `enclosure` 强制 length 属性而缩略图变体字节数未入库）。纯函数层 `src/lib/feed.ts`：`escapeXml`（五保留字符 + 剥离 XML 1.0 非法控制字符，不用 CDATA——防内容含 `]]>` 截断）与 `buildRssFeed(meta, items, now)`（now 注入可确定性测试；Invalid Date 的 pubDate 静默省略而非 `toUTCString` 抛 RangeError），13 个新单测锁定。路由复用 `listPhotos({ pageSize: 20 })`——feed 顺序=网站首页默认列表序（shotAt desc），DTO 层 exif 已归一无新增 `as` 转；条目摘要复用 exif-format 纯函数（日期 · 机身 · 镜头 · 焦段 · 光圈）；`guid`=sha1（isPermaLink="false"）；响应头 `Cache-Control: public, max-age=3600` 降低订阅器轮询 DB 压力。根布局 metadata 加 `alternates.types["application/rss+xml"]` 自动发现链接（用 `siteUrl()` 绝对地址——相对路径会被缺省 metadataBase 解析成 localhost）。130 测试全过、tsc/eslint 干净。

### ✅ 18. "隐藏 GPS" 隐私开关
- **为什么**：公开 API 目前全量返回 EXIF 含 GPS 坐标/地名（产品有意展示，但所有已发布照片的拍摄位置对爬虫完全公开）；个人照片场景应让站长可一键收敛。
- **怎么做**：设置项 `exposeGps`；关闭时公开 DTO 裁剪 gps 字段（admin 仍可见），ExifCard 不渲染地图/地名。
- **涉及**：`src/lib/queries.ts`、`src/lib/settings.ts`、`src/components/ExifCard.tsx`。
- **完成记录（2026-09-24）**：设置项 `exposeGps` 默认 `"true"`（保持现状公开，站长显式关闭才裁剪——升级零行为变化）。纯函数层 `geo.ts hideGps(exif)`：有 gps 返回 `{ ...exif, gps: undefined }` 裁剪副本（`gps: undefined` 经 JSON 序列化整键丢弃——`/api/photos` 响应面同样不含坐标）；无 gps/null 原引用返回（避免整批列表无谓拷贝），3 个新单测锁定（不可变/原引用/序列化面）。裁剪闸门收敛在 queries.ts **仅有的三个公开 exif 生产点**：`listPhotos`（首页/分类/标签/月份页 + /api/photos + feed.xml 全走它）、`listRandomPhotos`（随机漫游）、`getPhotoDetail`（详情页 + SEO metadata）——`canSeeGps = cache(exposeGps === "true" || isAdmin())` 请求级去重（同请求多生产点只读一次设置+会话；关闭时管理员仍可见），`applyGpsPolicy` 仅在需裁剪时浅拷贝。`listPhotosAdmin`/上传路由不经闸门（管理端全量）。**ExifCard/PhotoGrid 零改动**——渲染完全由 `exif?.gps` 数据驱动，上游裁剪后位置行自然消失（原清单预估需改 ExifCard 系过保守）。设置 API 布尔白名单 + SettingsClient 复选框（说明文案含原图字节注意事项）。**已知边界**：originalView 开启时原图路由服务的文件字节内嵌 EXIF GPS，DTO 裁剪无法触及——UI 文案已明示，强脱敏需同时关闭原图查看（记技术债）。133 测试全过、tsc/eslint 干净。
- **评审收编（2026-09-24，b8d69be 评审）**：L-1 `hideGps` 裁剪从 `{ ...exif, gps: undefined }` 改为**副本上 delete 整键移除**（`const rest = { ...exif }; delete rest.gps`，delete 只触碰新建副本、不可变语义等价；曾试解构 `const { gps: _gps, ...rest }` 写法，因 no-unused-vars 且 config-protection 禁改 eslint 配置而弃）——JSON.stringify 下两种裁剪等价（丢键），但 RSC flight 序列化把 undefined 编成 `"$undefined"` 占位符**保留键**跨 server→client 边界，旧写法裁剪对象到 client 后 `"gps" in exif` 仍为 true（值 undefined），键存在性判断不可靠；单测补断言 `hidden && "gps" in hidden === false` 锁定整键不存在。

---

## 已明确的技术债（非功能，随日常重构处理）

- ✅ `getSettings()` 加 React `cache()`（一次首页请求查 3 次 settings 表）—— 2026-09-24 完成：`getSettings = cache(readSettings)`；sync/instrumentation 等长驻后台路径改用 `readSettings` 原始读取，避免设置变更永不生效
- ✅ 筛选 URL 构造逻辑抽 `lib/filter-url.ts` 纯函数（page.tsx 与 Sidebar 两处重复且语义漂移）—— 2026-09-24 完成：`buildFilterUrl(current, patch, { unset: "clear" | "keep" })`，Sidebar 互斥语义=clear、chips 叠加语义=keep，12 个单测锁定行为
- DB Json 字段（exif）建 zod schema 读取校验，替代 `as` 双转
- `runSync`（195 行）拆分：`prebuildTaxonomies()` / `backfillExisting()` / `markMissing()`
- admin Client 共性抽取：`<ErrorBanner>` + `useAdminAction()`
- ViewMode 联合类型三处重复定义 → 复用 `ViewToggle` 导出的 `ViewMode`
- `eslint.config.mjs` 统一关闭 `@next/next/no-img-element`（项目明确不用 next/image，逐个 disable 是噪音）
- 强调色 `#f5b43c` 硬编码 5 处 → 提为 `--color-accent` token
- 无 `global-error.tsx`：根布局自身抛错仍白屏（error 边界不兜自己所在段的 layout）；需要时加最简自包含兜底页
- `listRandomPhotos` 候选池超 2 万张后偏向老照片（无 orderBy 的 take 近似主键序）；库规模逼近上限时改 COUNT + 随机 offset 分段取候选
- `getAdjacentPhotos` 66 行超 50 行准则——findNext/findPrev 两闭包可提为模块级函数；相邻 OR 条件构造是纯数据变换但无单测（bfa41fc 评审 L-5）
- `siteUrl()` 生产未配 SITE_URL 时静默回退 localhost，污染 canonical/OG/sitemap 三处 SEO 关键输出——应生产启动 fail-fast（对照 auth.ts SESSION_SECRET 先例；bfa41fc 评审 L-9）
- `resolvePhoto` 双查询（前缀 row + getPhotoDetail 重查同条）——published/missing 可并入 detail select 省一次查询（bfa41fc 评审 L-7）
- sitemap 三处 `.catch(() => [])` 静默降级（部分失败时 sitemap 残缺，搜索引擎可能视缺失 URL 为已删除）；引入日志设施后应记录（bfa41fc 评审 L-6）
- `parseYear` 的 `Number()` 强转怪癖：`"2e3"`→2000、`"0x7e4"`→2020 也能通过校验（在 1971..9998 界内、无 500 风险，但非严格四位年份）；需严格化时前置 `/^\d{4}$/`（第二轮评审 L-1）
- 移动端 chips 无单维度反选：再点已激活的分类/年份/标签 chip 不能取消该维度（只能回「全部」丢整组筛选）；`qs()`/`FilterLink` 需补 toggle-off 语义（第二轮评审 L-2）
- 标签横滑行规模化：标签多时横滑行过长，且首页载荷含双份 tags（Sidebar + 移动行）；规模上来后改 top N + 渐隐提示（第二轮评审 L-3）
- 移动端两行筛选栏缺 `nav` 地标（读屏无法跳转定位）；包 `<nav aria-label="…">`（第二轮评审 L-4）
- PhotoNav keydown effect 依赖整个 prev/next 对象与其余处 `next?.href` 稳定字符串风格不一；统一改 href 字符串依赖（第二轮评审 L-5）
- 归一化块（firstParam×N + parseYear + q trim/slice + month 正则）在首页/详情页重复；提取 `normalizeFilterParams(sp)` 共用；HomePage 组件约 240 行，chips/标题区可拆子组件（第二轮评审 L-7）
- nits：`ogDimensions` 的 `width <= 0` 守卫与 `!width` 冗余可简化；详情页 `Promise.all` 参数序与解构序不一致；`qRaw` 内联（第二轮评审 L-8）
- 原图文件 EXIF 脱敏：`exposeGps=false` 只裁剪 DTO/API 响应面，originalView 开启时原图路由下载的文件字节仍内嵌 GPS；如需强脱敏，应在同步/原图生成环节写回剥离 EXIF 的副本，或原图路由联动 exposeGps 关闭（功能 18 已知边界，2026-09-24 记录）
- feed.ts `media:title` 与 `media:content` 平级输出不符 MRSS 内容模型（应嵌套为 media:content 子元素；或删掉——与 `<title>` 恒同值冗余）（RSS 评审 L-1）
- feed.ts `ILLEGAL_XML_CHARS` 未覆盖 XML 1.0 非法非字符 U+FFFE/U+FFFF、U+FDD0–U+FDEF（MariaDB utf8mb4 可存储，实际出现概率极低）；`\x7F` 属合法保留正确、孤立代理对经 TextEncoder 变 U+FFFD 不产坏 XML（RSS 评审 L-2）
- feed.ts `now` 为 Invalid Date 时输出空 `<lastBuildDate></lastBuildDate>`（路由侧 `new Date()` 恒有效不可达，但与 item pubDate「无效即省略」策略不一致；可改条件 push + 补单测）（RSS 评审 L-3）
- feed pubDate 用 shotAt（拍摄时间）而非发布入库时间：补发老照片会被按 pubDate 排序/去重的阅读器视为旧条目漏报；与「feed 顺序=首页列表序」自洽，属产品取舍——路由注释应写明该决策（RSS 评审 L-4）
- feed.xml 路由 `force-dynamic` 对 GET route handler 在 Next 15+ 已冗余（默认即 dynamic）；保留属防御未来默认值变化，但非「必须」（RSS 评审 L-5）
- layout 静态 metadata 中 `siteUrl()` 模块加载时求值一次（canonical/OG/sitemap/feed 发现链接第 4 消费点）；全站 force-dynamic 下影响仅限构建期产物——并入既有「siteUrl() 生产 fail-fast」条目一并处理（RSS 评审 L-6）
- admin 图片页两处残留：`:47` 第二次 status `as` cast 冗余（`status === "all" ? undefined : status` TS 可自动收窄，与「消除 as」方向一致）；`?page=2.7` 非整数 → skip 40.8 → Prisma 500（既有问题，firstParam 收编后重复参数场景暴露面略增），`Math.trunc` 即可（第二轮评审 L-7）
- exif Json path 无索引：`listGear` 聚合与 make/model/lens 筛选均全表扫描；量级逼近万张时可建函数索引（`CAST(exif->>'$.make' AS CHAR)` 等三列）或落库时冗余抽列（功能 3，2026-09-24）
- 移动端无器材入口：Sidebar `hidden lg:block`，移动 chips 未加器材行（相机+镜头组合多、横滑行会过长）；需要时改折叠面板或搜索联想（功能 3，2026-09-24）
- 缩略图变体字节 EXIF 待实证：sharp 管线未调 withMetadata、display WebP 经理论推定已剥离 EXIF，但未对实际生成物字节验证；若仍含 GPS 则 `exposeGps=false` 下图片字节可泄露坐标——与「原图文件 EXIF 脱敏」条目并列处理（GPS 评审 M-1，2026-09-24）
- PhotoGrid 状态语义欠缺：加载/错误底部区域无 `role="status"`（读屏不播报加载结果）；「↑ 回到顶部」箭头字符未 aria-hidden（读屏读出"上箭头"）；focus ring `#f5b43c` 又硬编码 2 处（与「强调色提 token」条目同源）（无限滚动评审 L-2）
- `loading` 也在 `loadMore` deps 中（error 修复前的同款身份抖动）：每次 fetch 起止 loadMore 重建 → 底部 observer 重挂；功能上被 `if (loading || done) return` 守卫挡住无死循环，但可改 `loadingRef` 稳定 callback 身份消除重挂开销（无限滚动评审 L-3）
- `isAdmin()` 未做请求级缓存：详情页 resolvePhoto 与 canSeeGps 各读一次会话（canSeeGps 自身已 cache，isAdmin 的其余消费方仍重复解析 cookie）；可用 React `cache()` 包裹（GPS 评审 L-4）
- queries.ts `CARD_SELECT` 选了 `wallpaperSnapshot` 列但 DTO/组件均不消费（列表查询白传字节）；可从 select 移除（GPS 评审 L-6）
- `/api/photos` 公开只读端点无速率限制：无限滚动翻页每页直接打 DB；后续引入统一限流设施时并入（评审附注，2026-09-24）
- 器材历史未 trim 行的展示/筛选错位：`cleanGearValue` trim 后标签 vs `gearWhere` equals 原始存储值——侧栏计数 >0 但筛选结果为空，且仅 padding 不同的两分组 trim 后产生重复 React key（`cam-Canon|`）；写路径现已 trim 故新数据免疫，存量数据需清洗或冗余抽列方案（9d55ae2 评审 L-1，2026-09-25）
- `listGear` 聚合无 LIMIT 且首页无条件调用（移动端 Sidebar `hidden lg:block` 但数据仍序列化进 RSC payload）；若 lensModel 出现高基数脏数据，侧栏全量渲染无虚拟化（9d55ae2 评审 L-2，2026-09-25）
- `parseGearParam` 按 100 UTF-16 code unit 截断可能拆裂代理对（孤立 surrogate 只是 equals 失配，无害；与首页 `q` 的 `slice(0,64)` 同款既有姿态）（9d55ae2 评审 L-4，2026-09-25）
- PhotoGrid 重试按钮竞态窗口：`errorRef` 清零到 React flush 之间 IO 回调可与重试并发触发同一页请求各一次（sha1 去重幂等，仅浪费一次请求；快速双击不重复——离散事件同步 flush 后按钮已随 error 态卸载）（d9c901f 评审 L-5，2026-09-25）
- PhotoGrid 空首页死角：`initialItems` 为空且 `total>0`（listPhotos 的 count 与 findMany 并行窗口内发生删除）时渲染空态分支但无底部哨兵 → loadMore 不可达，需刷新才恢复；28e544d 起既有（d9c901f 评审 L-6，2026-09-25）
- 高德瓦片为非官方接口：无 SLA/限流承诺，可能失效或变更 URL；生产稳妥方案为天地图（免费 key，配 `MAP_TILE_GCJ02=false`）或自托管瓦片——env 逃生门已留（功能 1，2026-09-25）
- 移动端无 /map 入口：Sidebar `hidden lg:block`，ViewToggle 只承载视图参数切换不承载路由导航；需要时在首页移动端 chips 行加地图 chip（与「移动端无器材入口」同族）（功能 1，2026-09-25）
- 网格聚合边界伪影：恰跨网格边界的近邻点被拆为两簇（所有网格法固有）；视觉不可接受时改层次聚类或视口内重聚（功能 1，2026-09-25）
- `listMapPoints` 无分页：全量点位一次序列化进 RSC payload（每点约 200B，万张量级约 2MB）；规模逼近时改视口 bbox 查询或按地名聚合降载（功能 1，2026-09-25）
- Leaflet 默认白底弹窗与站点深色玻璃审美未完全统一（缩略图/气泡已深色，弹窗 chrome 是 Leaflet 默认白）；打磨时以 `.leaflet-popup-content-wrapper` 覆盖（功能 1，2026-09-25）
- 网格聚合未计 Mercator 纬度压缩与反经线：cellDeg 对经纬同取等角度，屏上经度间距实为 80px·cos(lat)（深圳纬度≈74px 无碍，60°N 处簇比预期松约 2 倍）；lon±179.9 邻近点落入相距最远的网格永不聚簇、fitBounds 亦绕地球一整圈——中国为中心的个人库下属外观问题，需要时改视口像素空间聚合（0830394 评审 L-3，2026-09-25）
- queries.ts 已达 716 行（距 800 上限 12%）：photos/gear/months/memories/map 查询集中一文件；下一个触碰它的功能应顺手拆 `src/lib/queries/` 按域分模块（0830394 评审 L-5，2026-09-25）
- ViewToggle 在非首页路由（/map、/archive/[year]）仍渲染但语义失效：(site) layout 头部的网格/日历切换在所有路由出现，点击会 replace 导航到 `/?view=` 静默离开当前页；打磨时按路由隐藏或为各页提供适配入口（0830394 评审 L-6、d770f8a 评审 L-2，2026-09-25）
- `listArchiveYear` 无行数上限：单年轻量行全量载入（对照 listPhotosCalendar 的 CALENDAR_MAX_PHOTOS=20000 上限）；每张照片百字节级、单年上万张才到 MB 量级，规模逼近时补 take 上限或按月分段查询（功能 16，2026-09-25）
- 归档页详情链接以全月为翻页上下文：`photoHref(sha1, {month})` 使详情页 ←/→ 在该月全集内翻页而非归档精选子集——精选是收藏优先序，与「归档内顺序浏览」的预期不完全一致；需要时改传精选 sha1 序列或新增归档专用上下文参数（功能 16，2026-09-25）
- 归档入口依赖年份筛选激活：「年度归档 →」仅在首页年份 h1 分支出现（且 total>0，评审 L-1），默认首页/移动端无直达入口（与「移动端无 /map 入口」同族）；需要时在 Sidebar 年份分组或移动端年份行加归档入口（功能 16，2026-09-25）
- displayMonthKey（IANA Asia/Shanghai）与 monthBounds/CONVERT_TZ（固定 +08:00）在 1986-1991 夏令时窗口有月归属分歧：中国那些年实行夏令时（夏季 +09:00），月末最后一天 UTC 15:00-15:59 拍摄的照片归档分组进 M+1，但「查看本月全部」的 /?month=M+1（固定偏移边界）不含它（实际落在 /?month=M）；listPhotosCalendar-vs-listMonths 是同族既有分歧、归档页成为第三消费方，年边界可证不受影响（DST 不跨 1 月 1 日）；个人库下属外观边缘，需要时统一为固定偏移或 IANA 单一出处（d770f8a 评审 L-4，2026-09-25）
